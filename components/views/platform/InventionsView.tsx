import React from 'react';
import Card from '../../Card';

const InventionsView: React.FC = () => {
    return (
        <Card title="Invention Registry">
            <p className="text-gray-400">A centralized repository for tracking and managing intellectual property and novel ideas generated within the platform.</p>
        </Card>
    );
};

export default InventionsView;
